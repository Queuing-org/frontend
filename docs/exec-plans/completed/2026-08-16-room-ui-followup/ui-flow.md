# UI Flow

- 데스크톱 상단 나가기 버튼 -> 기존 확인 다이얼로그 -> 확인 시 홈 이동, 취소 시 상단 버튼 포커스 복귀.
- 데스크톱 하단 X -> 프로필·큐·참가자·채팅 open 상태와 localStorage 값을 모두 false 처리.
- 전체 트랙 카드 -> 내 노래 8% 파란 배경 -> 현재 재생 상태가 있으면 회색 배경 우선.
- 방 프로필 -> 좌상단 `현재 큐잉 중` 헤더 -> 프로필 본문에는 중복 activity 문구 미표시.

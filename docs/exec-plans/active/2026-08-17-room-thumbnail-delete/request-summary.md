# Request Summary

- 방 수정의 썸네일 설정 UI/UX를 방 생성과 동일하게 만든다.
- 생성·수정 모두 X를 사용하지 않고 업로드/기본 이미지 카드로 선택한다.
- 수정에서 기본 이미지를 선택하고 저장하면 기존 썸네일을 삭제한다.
- 삭제 계약은 `DELETE /api/v2/rooms/{slug}/thumbnail`이다.
- 별도 확인 모달 없이 `편집 완료` 시 반영하고 모달 취소 시 draft를 폐기한다.

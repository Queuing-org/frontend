# UI Flow

1. 참가자 패널의 목록은 기존처럼 세로로 스크롤되지만 브라우저 scrollbar는 표시하지 않는다.
2. 데스크톱 하단 컨트롤 바 오른쪽 원형 새로고침 버튼을 누른다.
3. 현재 화면 밀도(normal 또는 compact)에 저장된 네 floating 패널 offset을 제거한다.
4. 열려 있는 패널은 닫히지 않고 즉시 각자의 기본 위치로 이동한다.
5. 닫힌 패널도 다음에 열었을 때 기본 위치에서 열린다.

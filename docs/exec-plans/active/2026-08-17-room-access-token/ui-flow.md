# UI Flow

- 기존 토큰이 있으면 메타 비밀번호와 무관하게 토큰으로 재입장한다.
- 토큰 재입장이 거부되면 토큰을 지우고 비공개 방은 비밀번호 입력, 공개 방은 최초 join을 다시 시도한다.
- `ROOM_INFO_UPDATED`는 현재 화면 메타를 즉시 보정한 뒤 authoritative meta를 재조회하고 동일 feedback key로 토스트를 하나만 유지한다.
- 소켓 연결 종료는 재접속 대기 상태로 전환하되 토큰을 보존한다.
- 명시적 leave·강퇴·세션 교체·방 삭제에서는 토큰과 방 캐시를 정리한다.

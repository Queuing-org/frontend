# Request Summary

기존 `dev` 브랜치에서 방 프로필·채팅·재생 아이콘의 비율을 정리하고, 설정의 닉네임·한 줄 메시지를 단일 저장 흐름으로 합치며, 방 생성 세부 설정과 방문 단계 재진입을 후속 개선한다. 백엔드 API와 공용 payload 타입은 유지한다. 전체 로컬 검증과 fresh QA 뒤 네 개의 로컬 커밋을 만들되 push, Draft PR #44 갱신, 새 PR 생성은 하지 않는다.

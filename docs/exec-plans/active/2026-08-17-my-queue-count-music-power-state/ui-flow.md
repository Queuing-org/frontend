# UI Flow

## 내 신청곡 개수

- 새로고침 직후 로딩 중 값을 성공 상태의 0개로 가장하지 않는다.
- queue 조회가 성공하면 서버에서 복원한 내 pending 개수를 표시한다.
- 개인 queue 응답이 없거나 실패한 동안에는 `…`를 유지한다.

## 음악력 버튼

- 기본 색, 더 진한 hover/focus 색, 현재 곡 선택 유지 색을 구분한다.
- 선택한 뒤에도 버튼은 활성 상태를 유지하고 재클릭은 기존 안내 로직으로 들어간다.
- current entry가 바뀌면 이전 entry의 선택 색은 제거한다.
